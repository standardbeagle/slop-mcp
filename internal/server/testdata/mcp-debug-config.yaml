servers:
  - name: "everything"
    prefix: ""
    transport: "stdio"
    command: "npx"
    args: ["-y", "@modelcontextprotocol/server-everything"]
    timeout: "60s"

proxy:
  health_check_interval: "30s"
  connection_timeout: "30s"
  max_retries: 3
